# OpenEnv-FragileChain Requirements
# Python 3.10+
# Install: pip install -r requirements.txt

# ────────────────────────────────────────────────────────────────────────────
# Core Runtime Dependencies
# ────────────────────────────────────────────────────────────────────────────

# OpenEnv framework (must be first for spec compliance)
openenv-core[core]>=0.2.1

# Web framework for API server
fastapi>=0.115.0
uvicorn[standard]>=0.24.0

# Data validation and serialization
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP client for baseline inference
requests>=2.31.0

# LLM client (for inference script and baseline)
openai>=1.0.0

# ────────────────────────────────────────────────────────────────────────────
# Development & Testing Dependencies (optional)
# For testing: pip install -r requirements.txt && pip install -r requirements-dev.txt
# Or: pip install -e ".[dev]"
# ────────────────────────────────────────────────────────────────────────────

# Uncomment to include testing dependencies:
# pytest>=8.0.0
# pytest-cov>=4.0.0
# httpx>=0.27.0

# ────────────────────────────────────────────────────────────────────────────
# Notes
# ────────────────────────────────────────────────────────────────────────────
# - openenv-core[core] provides the Environment base classes and spec validation
# - fastapi + uvicorn powers the REST API server (server/app.py)
# - pydantic handles all data models (models.py, Action, Observation, State)
# - openai client works with: Groq, HF Router, local Ollama, or OpenAI directly
# - requests is used by the grader for any future HTTP operations
#
# For Docker builds: server/requirements.txt is preferred (smaller image)
# For local development: use this root-level requirements.txt
